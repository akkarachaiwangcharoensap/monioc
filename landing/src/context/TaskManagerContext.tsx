/**
 * TaskManagerContext — landing-page stub.
 *
 * Only the Task interface is needed by landing-page components.
 * The full provider/hook implementation lives in the Tauri desktop app.
 */

import type { Task } from '@monioc/shared';

export type { Task };
